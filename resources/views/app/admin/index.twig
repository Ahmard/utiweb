{% extends 'layouts/admin.twig' %}

{% block content %}
    <div class="card">
        <div class="card-header left-border">Utility Web</div>
        <div class="card-body p-4 z-depth-1 font-italic left-border">
            <a href="{{ site.adminRoute('message', true) }}" class="list-group-item list-group-item-action d-flex justify-content-between">
                    <span>
                        <i class="fa fa-envelope"></i>
                        Messages
                    </span>
                <i class="fa fa-chevron-right"></i>
            </a>
            <a href="{{ site.adminRoute('error', true) }}" class="list-group-item list-group-item-action d-flex justify-content-between">
                    <span>
                        <i class="fa fa-bug"></i>
                        Errors & Exceptions
                    </span>
                <i class="fa fa-chevron-right"></i>
            </a>
        </div>

    </div>
{% endblock content %}